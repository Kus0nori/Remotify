<Application x:Class="Remotify.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Remotify"
             xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ContextMenu x:Key="TrayMenu">
            <MenuItem Header="Открыть" Click="OpenMenuItem_Click" FontWeight="Bold" />
            <Separator />
            <MenuItem Header="Выход" Click="ExitMenuItem_Click" />
        </ContextMenu>

        <tb:TaskbarIcon x:Key="TrayIcon"
                        ToolTipText="Remotify"
                        ContextMenu="{StaticResource TrayMenu}"
                        DoubleClickCommand="{Binding OpenCommand, Source={x:Static local:App.Current}}"
                        TrayLeftMouseDoubleClick="TrayIcon_TrayLeftMouseDoubleClick" />
    </Application.Resources>
</Application>
